# Application
spring.application.name=gold-business
server.port=8081

springdoc.api-docs.path=/v3/api-docs
 
# JWT
security.jwt.secret-key=${JWT_SECRET_KEY:change-me}
security.jwt.expiration-time=${JWT_EXPIRATION_TIME:36000000}
# Custom property for JWT issuer
security.jwt.issuer=${JWT_ISSUER:mon-serveur-auth}

app.encryption.key=${APP_ENCRYPTION_KEY:R29sZEJ1c2luZXNzS2V5MTI=}

# Notification settings
notification.email.from=${NOTIFICATION_EMAIL_FROM:your-email@example.com}
notification.email.sendgrid-api-key=${SENDGRID_API_KEY:change-me}
notification.push.fcm.credentials-path=${FIREBASE_CREDENTIALS_PATH:classpath:/firebase-service-account.json}
notification.sms.twilio.account-sid=${TWILIO_ACCOUNT_SID:change-me}
notification.sms.twilio.auth-token=${TWILIO_AUTH_TOKEN:change-me}
notification.sms.twilio.from=${TWILIO_FROM:+12314473797}

# Datasource - Use H2 in-memory for local development
# PostgreSQL/Neon (from Service_Connection_Variables.md)
# Host: ep-long-pond-avngc17e.c-11.us-east-1.aws.neon.tech
# Database: neondb
# User: neondb_owner
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-long-pond-avngc17e.c-11.us-east-1.aws.neon.tech:5432/neondb?sslmode=require}
spring.datasource.driverClassName=${SPRING_DATASOURCE_DRIVER:org.postgresql.Driver}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_G24FKVgWtpAn}
spring.jpa.database-platform=${SPRING_JPA_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
# Database schema sync is controlled by Docker/runtime env var to avoid accidental destructive changes.
# AUTO_UPDATE_DATABASE=true  -> Hibernate uses 'update'
# AUTO_UPDATE_DATABASE=false -> Hibernate uses 'validate' (safe default)
spring.jpa.hibernate.ddl-auto=${DB_HIBERNATE_DDL_AUTO:validate}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.flyway.enabled=false

# Redis (use REDIS_URL from Service_Connection_Variables.md by default)
# Override with environment variable `REDIS_URL` if needed.
spring.redis.url=${REDIS_URL:rediss://default:gQAAAAAAAtSlAAIgcDE2NTI4NDdmZTNhNTU0ZjU3Yjg2MjczZTk3NDk4YzI4YQ@destined-mackerel-185509.upstash.io:6379}
spring.data.redis.host=${SPRING_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_REDIS_PORT:6379}
spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD:}
spring.data.redis.timeout=5000ms

# Logging
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.data.redis=INFO

# 1. Désactivation de la création automatique des index (Elastic Cloud Serverless gère automatiquement)
spring.data.elasticsearch.auto-create-index=false
spring.data.elasticsearch.repositories.enabled=false

# Elasticsearch Cloud
spring.elasticsearch.uris=${SPRING_ELASTICSEARCH_URIS}
spring.elasticsearch.apikey=${SPRING_ELASTICSEARCH_APIKEY}
spring.elasticsearch.connection-timeout=10000
spring.elasticsearch.socket-timeout=60000
spring.elasticsearch.request-timeout=60000

# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:}
spring.security.oauth2.client.registration.google.redirect-uri=${GOOGLE_REDIRECT_URI:http://localhost:8081/login/oauth2/code/google}
spring.security.oauth2.client.registration.google.scope=openid, profile, email

spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=sub

# GitHub OAuth2
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID:}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET:}
spring.security.oauth2.client.registration.github.scope=read:user,user:email
spring.security.oauth2.client.registration.github.redirect-uri=${GITHUB_REDIRECT_URI:http://localhost:8080/login/oauth2/code/github}

# Kafka Authentication - Aiven.io
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS}
spring.kafka.security.protocol=${KAFKA_SECURITY_PROTOCOL}
spring.kafka.sasl.mechanism=${KAFKA_SASL_MECHANISM}
spring.kafka.sasl.jaas.config=${KAFKA_SASL_JAAS_CONFIG}
spring.kafka.ssl.enabled=true
app.kafka.enabled=${APP_KAFKA_ENABLED:true}
spring.kafka.enabled=${APP_KAFKA_ENABLED:true}
spring.kafka.ssl.trust-store-location=/home/nsangou/.kafka/aiven-truststore.jks
spring.kafka.ssl.trust-store-password=changeit
spring.kafka.properties.ssl.truststore.location=/home/nsangou/.kafka/aiven-truststore.jks
spring.kafka.properties.ssl.truststore.password=changeit
spring.kafka.properties.ssl.endpoint.identification.algorithm=

# Astra DB Configuration (from Service_Connection_Variables.md)
astra.db.api-endpoint=${ASTRA_DB_API_ENDPOINT:https://b833278d-e98e-4597-afa1-ad32b95d4da4-us-east-2.apps.astra.datastax.com}
astra.db.application-token=${ASTRA_DB_APPLICATION_TOKEN:}

spring.security.oauth2.client.provider.github.authorization-uri=https://github.com/login/oauth/authorize
spring.security.oauth2.client.provider.github.token-uri=https://github.com/login/oauth/access_token
spring.security.oauth2.client.provider.github.user-info-uri=https://api.github.com/user
spring.security.oauth2.client.provider.github.user-name-attribute=id

logging.file.name=logs/springboot.log
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n



